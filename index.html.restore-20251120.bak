<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="format-detection" content="telephone=no">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <title>Danica Silcox | Personal Website</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #FF69B4;
            --secondary-pink: #FFB6C1;
            --light-pink: #FFF0F5;
            --dark-pink: #DB7093;
            --text-color: #4A4A4A;
            --background-color: #FFF5F8;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
            -webkit-overflow-scrolling: touch;
        }

        body {
            font-family: 'Segoe UI', system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--background-color);
            color: var(--text-color);
            line-height: 1.6;
            overflow-x: hidden;
            -webkit-tap-highlight-color: transparent;
        }

        /* Reverted mobile touch-target overrides — keep default spacing */
        a, button {
            /* small padding left for visual balance; original desktop spacing restored */
            padding: 6px;
        }

        .container {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        section {
            padding: 4rem 2rem;
        }

        .profile-section {
            text-align: center;
            padding: 6rem 2rem 4rem;
            background: linear-gradient(135deg, var(--light-pink) 0%, var(--secondary-pink) 100%);
        }

        .profile-image {
            width: 400px;
            height: 400px;
            border-radius: 15px;
            object-fit: contain;
            border: 8px solid white;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
            margin-bottom: 2rem;
            position: relative;
            z-index: 2;
            transition: transform 0.3s ease;
        }
        
        .profile-image:hover {
            transform: scale(1.02);
        }

        .floating-element {
            position: absolute;
            animation: float 3s ease-in-out infinite;
            z-index: 1;
            opacity: 0.6;
        }

        .book {
            width: 40px;
            height: 50px;
            background: var(--primary-pink);
            clip-path: polygon(0 0, 100% 0, 100% 100%, 85% 100%, 85% 15%, 0 15%);
        }

        .music-note {
            font-family: Arial;
            color: var(--primary-pink);
            font-size: 2rem;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        .tennis-border {
            border: 10px solid transparent;
            border-image: url('data:image/svg+xml,<svg width="100" height="100" xmlns="http://www.w3.org/2000/svg"><circle cx="50" cy="50" r="45" fill="none" stroke="%23FFB6C1" stroke-width="10"/><path d="M5,50 C5,25 95,75 95,50 C95,75 5,25 5,50" fill="none" stroke="%23FFB6C1" stroke-width="5"/></svg>') 30 stretch;
            padding: 20px;
        }

        .piano-border {
            border: 10px solid transparent;
            border-image: url('data:image/svg+xml,<svg width="100" height="20" xmlns="http://www.w3.org/2000/svg"><rect width="100" height="20" fill="white"/><rect x="0" y="0" width="10" height="20" fill="black"/><rect x="20" y="0" width="10" height="20" fill="black"/><rect x="40" y="0" width="10" height="20" fill="black"/><rect x="60" y="0" width="10" height="20" fill="black"/><rect x="80" y="0" width="10" height="20" fill="black"/></svg>') 30 stretch;
            padding: 20px;
        }

        .interest-image {
            width: 100%;
            height: 300px;
            object-fit: cover;
            border-radius: 15px;
            box-shadow: 0 8px 20px rgba(0,0,0,0.1);
            margin-bottom: 1.5rem;
        }

        /* Remove extra spacing specifically for piano gallery images */
        .piano-gallery {
            display: flex;
            gap: 0;
            margin: 0;
        }
        .piano-gallery .interest-image {
            margin: 0;
            display: block;
            height: auto;
            width: 50%;
            border-radius: 0 0 10px 10px;
        }

        /* Remove spacing between tennis photos */
        .tennis-gallery {
            display: flex;
            gap: 0;
            margin: 0;
        }
        .tennis-gallery .interest-image {
            margin: 0;
            width: 50%;
            display: block;
        }
... (backup truncated)
